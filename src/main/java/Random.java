import java.util.concurrent.ThreadLocalRandom;

public class Random {
    int r = ThreadLocalRandom.current().nextInt(0, 76));


    /*
    Position bombPosition = new Position(r.nextInt(80), r.nextInt(24));
        terminal.setCursorPosition(bombPos */
}
